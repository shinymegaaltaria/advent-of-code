#include <iostream>
#include <fstream>
#include <map>
#include <vector>
using namespace std;
ifstream fin ("d13_processed.txt");
int main(){
    int n;
    cin >> n;
    long long int arr[n];
    for(int i = 0 ; i < n ; i++){
        cin >> arr[i];
    }
    
}